---
name: cli-policy-audit
description: Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.

## Quick install

```bash
npm install -g szroute   # or: npx szroute
szroute --version
```

## Subcommands

### `audit`

**Example:**

```bash
szroute audit
```

### `audit tail`

**Flags:**

- `--source <s>`
- `--follow`
- `--limit <n>`

**Example:**

```bash
szroute audit tail
```

### `audit search <query>`

**Flags:**

- `--source <s>`
- `--since <ts>`
- `--until <ts>`
- `--limit <n>`
- `--actor <id>`
- `--action <a>`

**Example:**

```bash
szroute audit search <query>
```

### `audit export <file>`

**Flags:**

- `--source <s>`
- `--format <f>`
- `--since <ts>`
- `--until <ts>`

**Example:**

```bash
szroute audit export <file>
```

### `audit stats`

**Flags:**

- `--source <s>`
- `--period <p>`

**Example:**

```bash
szroute audit stats
```

### `audit get <id>`

**Flags:**

- `--source <s>`

**Example:**

```bash
szroute audit get <id>
```

### `logs`

**Flags:**

- `--follow`
- `--filter <level>`
- `--lines <n>`
- `--timeout <ms>`
- `--base-url <url>`
- `--request-id <id>`
- `--api-key <key>`
- `--combo <name>`
- `--status <code>`
- `--duration-min <ms>`
- `--duration-max <ms>`
- `--export <path>`

**Example:**

```bash
szroute logs
```

### `policy`

**Example:**

```bash
szroute policy
```

### `policy list`

**Flags:**

- `--kind <k>`
- `--scope <s>`

**Example:**

```bash
szroute policy list
```

### `policy get <id>`

**Example:**

```bash
szroute policy get <id>
```

### `policy create`

**Example:**

```bash
szroute policy create
```

### `policy update <id>`

**Example:**

```bash
szroute policy update <id>
```

### `policy delete <id>`

**Flags:**

- `--yes`

**Example:**

```bash
szroute policy delete <id>
```

### `policy evaluate`

**Flags:**

- `--resource <r>`
- `--context <json>`

**Example:**

```bash
szroute policy evaluate
```

### `policy export <file>`

**Example:**

```bash
szroute policy export <file>
```

### `policy import <file>`

**Flags:**

- `--overwrite`

**Example:**

```bash
szroute policy import <file>
```

### `telemetry`

**Example:**

```bash
szroute telemetry
```

### `telemetry summary`

**Flags:**

- `--period <p>`
- `--compare-to <p>`

**Example:**

```bash
szroute telemetry summary
```

### `telemetry export`

**Flags:**

- `--out <path>`
- `--period <p>`

**Example:**

```bash
szroute telemetry export
```
